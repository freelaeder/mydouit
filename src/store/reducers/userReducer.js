// src/store/reducers/userReducer.js
const initialState = {
    // 用户信息
    user: {},
    name:{}
};

export default function userReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}